{
  "name": "sidra-os",
  "version": "1.0.0",
  "description": "Sidra OS",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @sidra/desktop tauri dev",
    "tauri": "pnpm --filter @sidra/desktop tauri",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
