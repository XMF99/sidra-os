{
  "name": "sidra-os",
  "version": "1.0.0",
  "description": "Sidra OS",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
