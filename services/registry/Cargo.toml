[package]
name = "sidra-registry"
version = "0.1.0"
edition = "2021"
description = "Sidra OS Registry & Standards Engine Service"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
sidra-domain = { path = "../../packages/domain" }
